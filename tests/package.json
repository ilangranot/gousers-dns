{
  "name": "gousers-tests",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test --config=web/playwright.config.ts",
    "test:e2e:ui": "playwright test --config=web/playwright.config.ts --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "playwright": "^1.44.0"
  }
}
